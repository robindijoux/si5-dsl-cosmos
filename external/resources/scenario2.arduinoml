application redButton

# Declaring bricks
sensor buttonOne: 9
sensor buttonTwo: 8
actuator buzzer: 12

# Declaring states
on {
    led <= HIGH
    buttonOne is HIGH and buttonTwo is HIGH => off
}

-> off {
    led <= LOW
    buttonOne is HIGH and buttonTwo is HIGH => on
}
