application pushbutton

# Declaring bricks
sensor button: 9
actuator led: 12
actuator buzzer : 11

# Declaring states
on {

    buzzer <= HIGH
    button is HIGH => off
}

-> off {
    buzzer <= LOW
    led <= HIGH
    button is HIGH => restart
}

-> restart {
    led <= LOW
    buzzer <= LOW
    button is HIGH => on
}